{
  "name": "splatter",
  "version": "0.1.0",
  "description": "Mac-local iPhone video to Gaussian Splat GitHub Pages pipeline",
  "private": true,
  "bin": {
    "splatter": "bin/splatter"
  },
  "scripts": {
    "check": "zsh -n scripts/*.sh bin/splatter && node --check public/main.js && node --check scripts/viewer_qa.mjs && scripts/validate_public.sh && bin/splatter openmvs-validate && bin/splatter viewer-qa && scripts/test_cli.sh",
    "serve": "python3 -m http.server 8080 --directory public"
  },
  "devDependencies": {
    "@playcanvas/splat-transform": "^2.0.6"
  }
}
